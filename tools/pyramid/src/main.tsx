import React from 'react'
import ReactDOM from 'react-dom/client'
import PyramidCalculator from './PyramidCalculator'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <PyramidCalculator />
    </React.StrictMode>,
)
